<VirtualHost *:443>

        SSLEngine on
        SSLCertificateFile ${cert_file}
        SSLCertificateKeyFile ${key_file}
        DocumentRoot ${web_root}
        CustomLog ${log_dir}/https-access.log combined
        ErrorLog ${log_dir}/https-error.log
        Include conf.https.d/*.conf

</VirtualHost>